{
  "version": "2.1.0",
  "scripts": {
    "server": "npx http-server src -a 0.0.0.0 -p 8080 -c-1",
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "prettier-plugin-astro": "^0.14.1"
  },
  "dependencies": {
    "astro": "^5.16.4"
  }
}
